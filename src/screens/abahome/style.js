import { COLORS, FONT_SIZE } from "../../constants/theme";

export const styles = {
    container: {
        backgroundColor: COLORS.white,
        flex: 1,
        width:"100%",        
    },
    text: {
        fontSize: FONT_SIZE.md,
        color: COLORS.gray1,
        textAlign:"center",
        marginBottom: 10,
        marginLeft: 10
    }
}